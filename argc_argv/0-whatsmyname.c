#include <stdio.h>
#include "main.h"

/**
 * main - printing own name with new line
 * @argc: argument count
 * @agrv: argument vector
 * Return: Always 0 (Success)
 *
 */

{
	printf("%s\n", *argv);

	return (0);
}
